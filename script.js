function simulateStaking() {
    var stakeAmount = prompt("Enter the amount of ADA you want to stake:");
    var reward = stakeAmount * 0.05;  // Assume a 5% reward for demo purposes
    alert("You've earned " + reward + " CNC tokens for staking " + stakeAmount + " ADA.");
  }
  